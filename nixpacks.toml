[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["python3 -m pip install --break-system-packages -r bot/requirements.txt"]

[start]
cmd = "python3 bot/main.py"
